# OpenAI API key used by the portfolio chatbot (Step 4).
# Must be prefixed with PUBLIC_ so Astro exposes it to the browser island.
# NOTE: this key is shipped to the client. Set hard usage limits in the
# OpenAI dashboard (Billing > Usage limits) before deploying publicly.
PUBLIC_OPENAI_API_KEY=your_openai_api_key_here
